# Add the ID_INPUT_JOYSTICK attribute to the device so SDL picks up on it

# 8Bitdo N30 Pro 2 GamePad Bluetooth mode(START)
SUBSYSTEM=="input", ATTRS{name}=="8BitDo N30 Pro 2", MODE="0666", ENV{ID_INPUT_JOYSTICK}="1"
